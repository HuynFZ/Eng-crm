services:
  mysql:
    image: mysql:8.4.7
    container_name: mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: english_crm
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - database-mysql:/var/lib/mysql

volumes:
  database-mysql: